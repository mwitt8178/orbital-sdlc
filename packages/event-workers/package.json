{
  "name": "@orbital/event-workers",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "SNS/SQS consumer Lambdas: ws-fanout, audit-indexer, defect-router, memory-recorder, replay-recorder. Each is independently bundleable. Imports types, auth, db, domain only — never orchestrator-daemon or api-lambda.",
  "main": "dist/index.js",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "test": "vitest run",
    "lint": "eslint src"
  },
  "dependencies": {
    "@orbital/auth": "*",
    "@orbital/db": "*",
    "@orbital/domain": "*",
    "@orbital/types": "*",
    "@aws-sdk/client-apigatewaymanagementapi": "^3.750.0",
    "drizzle-orm": "^0.36.0",
    "postgres": "^3.4.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "@types/node": "^22.10.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
